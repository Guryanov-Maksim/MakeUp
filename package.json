{
  "name": "makeup",
  "version": "1.0.0",
  "description": "",
  "main": "./src/index.js",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "preview": "vite preview",
    "start": "vite",
    "start:backend": "fastify start server/index.js -w -P -V -l info --port 3000 -a 0.0.0.0",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fastify": "^4.17.0",
    "fastify-cli": "^5.7.1"
  },
  "devDependencies": {
    "vite": "^4.3.5"
  }
}
